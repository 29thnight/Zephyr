fn classify(x: int) -> string {
    return match x {
        0 => "zero",
        1 | 2 | 3 => "small",
        n if n < 0 => "negative",
        _ => "large",
    };
}

fn main() {
    print(classify(0));
    print(classify(2));
    print(classify(-5));
    print(classify(100));
}
