trait Greet {
    fn greet(self) -> string;
}

struct Cat { name: string }

impl Greet for Cat {
    fn greet(self) -> string {
        return f"Meow, I am {self.name}";
    }
}

fn main() {
    let c = Cat { name: "Kitty" };
    print(c.greet());
}
