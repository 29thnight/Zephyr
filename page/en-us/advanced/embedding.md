# Embedding ZephyrVM

> [!NOTE] C++ Internal Bridges
> The Zephyr pipeline is structurally built as an Embedding Framework for Host Engines.

In-depth documentation exploring the standalone `ZephyrVM` instance initializations, raw byte-evaluations, and troubleshooting mechanisms are slated for release soon.
