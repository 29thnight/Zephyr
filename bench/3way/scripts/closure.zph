fn make_adder(base: int) -> Function {
    return fn(value: int) -> int {
        return value + base;
    };
}

fn main() {
    let mut total: int = 0;
    let mut i: int = 0;
    while i < 100000 {
        let add_i = make_adder(i);
        total = total + add_i(1);
        i = i + 1;
    }
}
